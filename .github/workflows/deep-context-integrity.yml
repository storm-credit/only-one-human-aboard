name: Deep Context Compile + Integrity

permissions:
  contents: write

on:
  push:
    branches:
      - prewriting/deep-context-410
    paths:
      - 'docs/prewriting-v3/EPISODE-BLUEPRINT-ACT*-v0.1.md'
      - 'docs/obsidian-v3/deep-contexts/**'
      - 'docs/obsidian-v3/contexts/**'
      - 'tools/compile_deep_contexts.py'
      - 'tools/deep_context_integrity.py'
      - '.github/workflows/deep-context-integrity.yml'
  pull_request:
    branches: [main]
    paths:
      - 'docs/prewriting-v3/EPISODE-BLUEPRINT-ACT*-v0.1.md'
      - 'docs/obsidian-v3/deep-contexts/**'
      - 'docs/obsidian-v3/contexts/**'
      - 'tools/compile_deep_contexts.py'
      - 'tools/deep_context_integrity.py'
      - '.github/workflows/deep-context-integrity.yml'

jobs:
  compile-and-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Compile Deep Projected Context 410/410
        run: python tools/compile_deep_contexts.py --preserve-through 28

      - name: Validate full Deep Projected Context contract
        run: python tools/deep_context_integrity.py

      - name: Verify PR contains current compiled output
        if: github.event_name == 'pull_request'
        run: git diff --exit-code -- docs/obsidian-v3/deep-contexts

      - name: Commit compiled Deep Context on feature push
        if: github.event_name == 'push'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- docs/obsidian-v3/deep-contexts
          if git diff --cached --quiet; then
            echo "Deep Context output already current."
          else
            git commit -m "Compile Deep Context 410/410 from Blueprint authority"
            git push origin HEAD:prewriting/deep-context-410
          fi
